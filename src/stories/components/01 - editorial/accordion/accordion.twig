{{ attach_library('coba/accordion') }}

<div class="mb-4 {{ modifier }}">

  <div class="accordion">
    {% block accordion_items %}
      {% for item in items %}
        {%
          include '@coba/01 - editorial/accordion/accordion-item.twig' with { item: item } only
        %}
      {% endfor %}
    {% endblock %}
  </div>
</div>
